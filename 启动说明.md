# Letta 服务器启动方式说明

现在您有多种方式可以启动 Letta 服务器，而不需要使用 `uv run letta server` 命令。

## 🚀 启动方式

### 1. 简单启动（推荐）
```bash
python start_letta.py
```
这是最简单的启动方式，使用默认配置启动服务器。

### 2. 带参数启动
```bash
python run_server.py --help          # 查看所有选项
python run_server.py                 # 默认启动
python run_server.py --port 8080     # 指定端口
python run_server.py --host 0.0.0.0  # 允许外部访问
python run_server.py --debug         # 调试模式
python run_server.py --reload        # 热重载模式
```

### 3. 直接运行 main.py
```bash
python main.py                       # 直接启动服务器
python main.py --help               # 查看帮助信息
```

## 📝 文件说明

- **`start_letta.py`** - 最简单的启动脚本，适合快速启动
- **`run_server.py`** - 功能完整的启动脚本，支持各种命令行参数
- **`main.py`** - 修改后的主文件，现在支持直接运行

## 🔧 默认配置

- **主机地址**: localhost (127.0.0.1)
- **端口**: 8283
- **调试模式**: 关闭
- **热重载**: 关闭

## 🌐 访问服务器

服务器启动后，您可以通过以下地址访问：
- 本地访问: http://localhost:8283
- 管理界面: https://app.letta.com/development-servers/local/dashboard

## ⚠️ 注意事项

1. 确保您在项目根目录（`/home/zy/letta`）下运行这些脚本
2. 确保所有依赖项已正确安装
3. 如果遇到导入错误，请检查 Python 路径和依赖安装
4. 使用 Ctrl+C 可以优雅地停止服务器

## 🔄 从旧方式迁移

如果您之前使用：
```bash
uv run letta server
```

现在可以使用：
```bash
python start_letta.py
# 或
python run_server.py
# 或
python main.py
```

这些新方式提供了相同的功能，但不需要通过 uv 运行。

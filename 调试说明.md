# Letta 项目调试指南

## 🐛 调试配置说明

我已经为您创建了完整的VSCode调试配置，包含多种调试场景。

## 📋 可用的调试配置

### 1. 🚀 调试 Letta 服务器 (简单启动)
- **用途**: 使用最简单的方式调试服务器
- **启动文件**: `start_letta.py`
- **特点**: 默认配置，快速启动

### 2. 🔧 调试 Letta 服务器 (带参数)
- **用途**: 带调试和热重载功能的服务器调试
- **启动文件**: `run_server.py`
- **参数**: `--debug --reload --port 8283`
- **特点**: 支持代码修改后自动重启

### 3. ⚡ 调试 Letta 服务器 (main.py)
- **用途**: 通过main.py调试服务器
- **启动文件**: `main.py`
- **特点**: 使用修改后的main.py启动

### 4. 🔍 调试 Letta 服务器 (官方方式)
- **用途**: 使用官方模块方式调试
- **启动方式**: `python -m letta server --debug`
- **特点**: 官方推荐的调试方式

### 5. 🌐 调试 Letta 服务器 (外部访问)
- **用途**: 允许外部设备访问的调试模式
- **参数**: `--host 0.0.0.0 --port 8283 --debug`
- **特点**: 适合网络调试

### 6. 🧪 调试测试
- **用途**: 调试单个测试文件
- **使用方式**: 打开测试文件后使用此配置
- **特点**: 使用pytest运行当前文件

### 7. 📋 调试当前文件
- **用途**: 调试当前打开的Python文件
- **特点**: 通用的文件调试配置

### 8. 🐛 调试 Letta CLI
- **用途**: 调试Letta的命令行工具
- **特点**: 可以自定义CLI参数

## 🚀 如何使用

### 方法1: 通过调试面板
1. 打开VSCode
2. 按 `Ctrl+Shift+D` 打开调试面板
3. 在顶部下拉菜单中选择想要的调试配置
4. 点击绿色播放按钮开始调试

### 方法2: 通过快捷键
1. 按 `F5` 使用默认调试配置
2. 按 `Ctrl+F5` 运行而不调试

### 方法3: 通过命令面板
1. 按 `Ctrl+Shift+P` 打开命令面板
2. 输入 "Debug: Select and Start Debugging"
3. 选择想要的调试配置

## 🔧 调试技巧

### 设置断点
- **行断点**: 在代码行号左侧点击红点
- **条件断点**: 右键点击断点，设置条件
- **日志断点**: 右键点击断点，设置日志输出

### 调试控制
- **F5**: 继续执行
- **F10**: 单步跳过 (Step Over)
- **F11**: 单步进入 (Step Into)
- **Shift+F11**: 单步跳出 (Step Out)
- **Ctrl+Shift+F5**: 重新启动调试

### 查看变量
- **变量面板**: 查看当前作用域的所有变量
- **监视面板**: 添加自定义表达式监视
- **调用堆栈**: 查看函数调用链

## 🛠️ 环境配置

### 环境变量
调试配置中已设置以下环境变量：
- `PYTHONPATH`: 项目根目录
- `COMPOSIO_DISABLE_VERSION_CHECK`: 禁用版本检查

### Python路径
确保VSCode使用正确的Python解释器：
1. 按 `Ctrl+Shift+P`
2. 输入 "Python: Select Interpreter"
3. 选择项目对应的Python环境

## 📝 常见调试场景

### 调试服务器启动问题
使用 "🚀 调试 Letta 服务器 (简单启动)" 配置，在启动代码处设置断点。

### 调试API请求处理
使用 "🔧 调试 Letta 服务器 (带参数)" 配置，在API处理函数中设置断点。

### 调试数据库操作
在数据库相关代码中设置断点，使用任一服务器调试配置。

### 调试测试用例
打开测试文件，使用 "🧪 调试测试" 配置。

## ⚠️ 注意事项

1. **依赖安装**: 确保所有依赖项已正确安装
2. **工作目录**: 确保在项目根目录下调试
3. **端口占用**: 如果8283端口被占用，修改配置中的端口号
4. **权限问题**: 某些操作可能需要管理员权限

## 🔍 故障排除

### 调试无法启动
1. 检查Python解释器路径
2. 确认依赖项安装完整
3. 查看调试控制台的错误信息

### 断点不生效
1. 确认 `justMyCode` 设置为 `false`
2. 检查文件路径是否正确
3. 重新启动调试会话

### 变量显示异常
1. 检查变量作用域
2. 使用监视面板添加表达式
3. 查看调用堆栈确认执行位置
